world:
  height: 50  # the height of the world
  width: 50   # the height of the world
  step_time: 0.1  # 10Hz calculate each step
  sample_time: 0.1  # 10 Hz for render and data extraction 
  offset: [0, 0] # the offset of the world on x and y 
  collision_mode: 'reactive'  # 'stop', 'unobstructed', 'reactive'
  control_mode: 'keyboard'  # 'keyboard', 'auto'
  obstacle_map: 'assets/simple.jpg'

robot:
  - kinematics: {name: 'acker'}  # omni, diff, acker
    shape: {name: 'rectangle', length: 1, width: 1, wheelbase: 1}
    state: [18, 40, 0, 0]
    goal: [40, 40, 0]
    vel_max: [1, 1]
    vel_min: [-1, -1]
    behavior: {name: 'dash'} # move toward to the goal directly
    plot:
      show_trail: True
      traj_color: 'g'
